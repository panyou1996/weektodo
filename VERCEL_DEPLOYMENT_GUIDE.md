# Vercel 部署指南

本项目已配置好 Vercel 部署所需的所有文件。按照以下步骤即可将 WeekToDo 部署到 Vercel。

## 📋 已完成的配置

✅ **vercel.json** - Vercel 部署配置文件  
✅ **package.json** - 添加了 `vercel-build` 脚本  
✅ **vue.config.js** - 配置了 `publicPath: './'`  
✅ **.vercelignore** - 排除不需要部署的文件  
✅ **.env.example** - 环境变量模板  

## 🚀 部署步骤

### 方法一：通过 Vercel Dashboard（推荐，最简单）

1. **访问 Vercel 官网**
   - 打开 https://vercel.com
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 "Add New..." → "Project"
   - 选择 "Import Git Repository"
   - 选择您的仓库：`panyou1996/weektodo`

3. **配置项目**（通常自动检测，无需修改）
   - **Framework Preset**: Vue.js（自动检测）
   - **Build Command**: `npm run build` 或 `yarn build`
   - **Output Directory**: `dist`（自动设置）
   - **Install Command**: `npm install` 或 `yarn install`

4. **环境变量**（可选）
   - 如果需要 Sentry 错误追踪，添加：
     - `VUE_APP_SENTRY_DNS` = 您的 Sentry DSN

5. **点击 Deploy**
   - Vercel 会自动构建和部署
   - 首次部署大约需要 2-3 分钟

6. **获取部署 URL**
   - 部署成功后，您会得到一个 URL：
     - 例如：`https://weektodo-xxx.vercel.app`
   - 可以在设置中添加自定义域名

### 方法二：通过 Vercel CLI

1. **安装 Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **登录**
   ```bash
   vercel login
   ```

3. **部署**
   ```bash
   cd c:\Users\panyou\Documents\weektodo
   vercel
   ```

4. **生产环境部署**
   ```bash
   vercel --prod
   ```

## 🔧 配置说明

### vercel.json 配置解析

```json
{
  "version": 2,
  "name": "weektodo",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",  // 静态网站构建
      "config": {
        "distDir": "dist"              // 输出目录
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",                  // 所有路由
      "dest": "/index.html"            // 指向 index.html（SPA 路由）
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"                 // 允许跨域请求
        }
      ]
    }
  ]
}
```

### 构建脚本

package.json 中添加了：
```json
"scripts": {
  "vercel-build": "vue-cli-service build"
}
```

Vercel 会自动调用此脚本进行构建。

## 📝 部署后配置

### 1. 自定义域名（可选）

在 Vercel Dashboard 中：
1. 进入您的项目
2. 点击 "Settings" → "Domains"
3. 添加您的域名
4. 按照提示配置 DNS

### 2. 环境变量

如果将来添加 Supabase 同步功能，需要在 Vercel 中添加环境变量：

1. 进入项目 Settings → Environment Variables
2. 添加：
   - `VUE_APP_SUPABASE_URL`
   - `VUE_APP_SUPABASE_ANON_KEY`

### 3. 自动部署

Vercel 已自动配置 GitHub 集成：
- 每次 push 到 `main` 分支 → 自动部署到生产环境
- 每次 push 到其他分支 → 自动创建预览部署
- Pull Request → 自动创建预览链接

## ✅ 验证部署

部署成功后，访问您的 Vercel URL，检查：

- ✅ 页面正常加载
- ✅ 可以创建待办事项
- ✅ 数据存储在浏览器本地（IndexedDB）
- ✅ 主题切换正常工作
- ✅ 导出/导入功能正常

## 🔍 常见问题

### 1. 构建失败

**问题**：依赖安装失败  
**解决**：检查 package.json 中的依赖版本是否正确

**问题**：构建超时  
**解决**：Vercel 免费版构建时间限制为 45 分钟，通常足够

### 2. 页面空白

**问题**：路由配置错误  
**解决**：确保 vercel.json 中的 routes 配置正确

### 3. 资源 404

**问题**：publicPath 配置错误  
**解决**：确保 vue.config.js 中设置了 `publicPath: './'`

## 🎯 性能优化建议

1. **启用缓存**
   - Vercel 自动为静态资源添加缓存头

2. **压缩**
   - Vercel 自动启用 Gzip 和 Brotli 压缩

3. **CDN**
   - Vercel 使用全球 CDN，自动加速

## 📊 监控和分析

在 Vercel Dashboard 中可以查看：
- 部署历史
- 访问统计
- 性能指标
- 错误日志

## 🔗 相关链接

- [Vercel 文档](https://vercel.com/docs)
- [Vue.js 部署指南](https://cli.vuejs.org/guide/deployment.html#vercel)
- [项目仓库](https://github.com/panyou1996/weektodo)

## 📞 需要帮助？

如果遇到问题：
1. 查看 Vercel 构建日志
2. 检查浏览器控制台错误
3. 参考本文档的常见问题部分

---

**准备好了吗？** 现在就去 [Vercel Dashboard](https://vercel.com/dashboard) 导入您的项目吧！🚀
